import styled from '@emotion/styled';

const Heading = styled.h1`
  color: red;
`;

const Home = () => {
  return (
    <div>
      <Heading>Soy un header</Heading>
    </div>
  );
};

export default Home;
